<?xml version="1.0" encoding="utf-8"?>
<merge xmlns:android="http://schemas.android.com/apk/res/android">

    <View
        android:id="@+id/tab_divider"
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:layout_above="@+id/tab"
        android:background="@color/dark_grey_border" />

    <LinearLayout
        android:id="@+id/tab"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:layout_alignParentBottom="true"
        android:orientation="horizontal">

        <FrameLayout
            android:id="@+id/tab_me"
            style="@style/home_tab_section">

            <com.devspark.robototextview.widget.RobotoTextView
                style="@style/home_tab_icon"
                android:drawableTop="@drawable/tab_icon_me"
                android:text="@string/tab_me" />
        </FrameLayout>

        <FrameLayout
            android:id="@+id/tab_report"
            style="@style/home_tab_section">

            <com.devspark.robototextview.widget.RobotoTextView
                style="@style/home_tab_icon"
                android:drawableTop="@drawable/tab_icon_report"
                android:text="@string/tab_report" />
        </FrameLayout>
    </LinearLayout>

</merge>
